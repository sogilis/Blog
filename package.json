{
  "name": "sogilis-blog",
  "version": "1.0.0",
  "description": "Sogilis blog",
  "repository": "sogilis/Blog",
  "scripts": {
    "start": "hugo -d ../dist -s site -vw",
    "build": "rimraf dist && hugo -d ../dist -s site -v",
    "build:preview": "yarn run build -- -D -F"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^6.1.0",
    "rimraf": "^3.0.0"
  },
  "dependencies": {
    "hugo-bin": "^0.47.0",
    "netlify-cms": "^2.10.16"
  }
}
